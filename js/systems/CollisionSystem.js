export class CollisionSystem{static circleRect(x,y,r,o){const qx=Math.max(o.x,Math.min(x,o.x+o.w)),qy=Math.max(o.y,Math.min(y,o.y+o.h));return(x-qx)**2+(y-qy)**2<r*r}static hit(x,y,r,objects){return objects.find(o=>this.circleRect(x,y,r,o))||null}static move(entity,dx,dy,objects){let hit=null;const hx=this.hit(entity.x+dx,entity.y,entity.r,objects);if(!hx)entity.x+=dx;else hit=hx;const hy=this.hit(entity.x,entity.y+dy,entity.r,objects);if(!hy)entity.y+=dy;else hit=hy;return hit}}
